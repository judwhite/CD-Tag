<view:WindowViewBase 
    x:Class="CDTag.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:Tag="clr-namespace:CDTag.Views.Tag" 
    xmlns:view="clr-namespace:CDTag.Views" 
    Title="CD-Tag" 
    Height="700" 
    Width="1024"
    MinHeight="400"
    MinWidth="600"
    Background="#FFBCDDFD"
    WindowStartupLocation="CenterScreen"
    x:Name="mainWindow"
    view:AttachedProperties.RegisterCommandBindings="{Binding CommandBindings}"
>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="ErrorItems" Grid.Row="0" />
        <Tag:TagView Grid.Row="1" />
        
    </Grid>
</view:WindowViewBase>
